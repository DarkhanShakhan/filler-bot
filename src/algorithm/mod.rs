use crate::player::*;

pub fn find_available_options(anfield:&Vec<String>, piece:&Vec<String>, player: Player) -> Vec<(i32,i32)> {
    match player {
        Player1 => {},
        Player2 => {}
    }
    vec![]
}